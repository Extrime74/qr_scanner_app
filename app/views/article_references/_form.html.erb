<%= form_with(model: article_reference) do |form| %>
  <% if article_reference.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(article_reference.errors.count, "error") %> prohibited this article_reference from being saved:</h2>

      <ul>
        <% article_reference.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :article, style: "display: block" %>
    <%= form.text_field :article %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
